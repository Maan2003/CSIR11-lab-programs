#include <stdio.h>

int main() {
        int a;
        printf("Enter number: ");
        scanf("%d", &a);

        int result = 2 * a + 1;
        printf("The result is %d\n", result);
        return 0;
}